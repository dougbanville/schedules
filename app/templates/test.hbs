{{#each model as |station|}}
    <h1>{{station.name}}</h1>
    <h2>{{station.slot.length}}</h2>

    {{#each station.slot as |sl|}}
    <h2>{{sl.title}}</h2>
    <h3>{{sl.start}} - {{sl.end}}</h3>
    {{/each}}
{{/each}}
{{outlet}}
